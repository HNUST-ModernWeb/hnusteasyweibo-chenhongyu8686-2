spring:
  datasource:
    url: jdbc:mysql://localhost:3306/social_platform?useSSL=false&serverTimezone=UTC
    username: root
    password: 719937860chy
    driver-class-name: com.mysql.cj.jdbc.Driver

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.backend.entity